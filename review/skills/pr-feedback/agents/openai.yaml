interface:
  display_name: "PR Feedback"
  short_description: "Triage GitHub PR review feedback into fix and skip lists"
  default_prompt: "Use $pr-feedback to analyze the current PR's review feedback."
policy:
  allow_implicit_invocation: true
