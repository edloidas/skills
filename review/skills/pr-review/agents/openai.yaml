interface:
  display_name: "PR Review"
  short_description: "Review a pull request, or verify and answer the feedback left on yours"
  default_prompt: "Use $pr-review on the current pull request. Review it if it is someone else's, otherwise verify and answer the open threads. Do not change code."
policy:
  allow_implicit_invocation: false
