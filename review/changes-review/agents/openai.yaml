interface:
  display_name: "Changes Review"
  short_description: "Review changed code with repo-native checks, convention analysis, and logic-focused findings."
  default_prompt: "Use $changes-review to inspect changed files, run the repo's preferred quick checks with autofix if available, and report logic or behavior issues."
policy:
  allow_implicit_invocation: false
