interface:
  display_name: "NPM Release"
  short_description: "Prepare and execute npm or pnpm releases with explicit approval before pushing."
  default_prompt: "Use $npm-release to validate release readiness, recommend a version bump, and pause for explicit approval before pushing tags or publishing."
policy:
  allow_implicit_invocation: false
