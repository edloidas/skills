interface:
  display_name: "Bro"
  short_description: "Say the last message again, straight — shorter, plainer, decision last"
  default_prompt: "Use $bro to rework the last answer: bottom line first, only what matters, and anything I have to decide at the end."
policy:
  allow_implicit_invocation: false
