interface:
  display_name: "Fix Findings"
  short_description: "Triage review or debugging findings into tasks and fix them"
  default_prompt: "Use $fix-findings to triage the problems raised in this conversation into tasks and work through them."
policy:
  allow_implicit_invocation: true
