# <repo name>

<One or two sentences: what this repo is and what it produces. No history, no roadmap.>

## Commands

<Only the commands that are not discoverable from package.json / Makefile / build file, or
whose correct usage is non-obvious. Delete this section if there are none.>

| Command | What it does |
| ------- | ------------ |
| `<cmd>` | <effect>      |

## Structure

<Only the directories whose purpose a newcomer cannot guess from the name. Delete the
section rather than restating an obvious tree.>

## Conventions

<Local decisions a model would otherwise get wrong: which library to reach for, naming,
where new code goes, what is generated and must not be hand-edited.>

## Git & GitHub

<Commit message format, branch naming, PR body rules — only where they deviate from the
default the agent would pick.>
