interface:
  display_name: "Labels Sync"
  short_description: "Check, apply, or export GitHub labels for the current repo"
  default_prompt: "Use $labels-sync to compare the current repository labels against the bundled standard label set, apply changes, or export the repo's current labels as reusable JSON."
policy:
  allow_implicit_invocation: false
