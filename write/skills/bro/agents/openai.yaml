interface:
  display_name: "Bro"
  short_description: "Say the last message again, straight — short, structured, skimmable"
  default_prompt: "Use $bro to rework the last answer into a bottom line, a few bullets of what matters, and concrete next steps."
policy:
  allow_implicit_invocation: false
