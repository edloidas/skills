interface:
  display_name: "Lint Sync"
  short_description: "Compare ESLint rules against Biome coverage and migration options"
  default_prompt: "Use $lint-sync to compare active ESLint rules against Biome coverage before changing lint configuration."
policy:
  allow_implicit_invocation: true
