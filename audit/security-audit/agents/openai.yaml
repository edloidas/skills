interface:
  display_name: "Security Audit"
  short_description: "Audit GitHub Actions, release config, and repo settings for supply-chain risks"
  default_prompt: "Use $security-audit to scan this repo for supply-chain risks in GitHub Actions workflows, release configuration, and repository settings (rulesets, Actions defaults, secret scanning)."
policy:
  allow_implicit_invocation: true
