interface:
  display_name: "Security Audit"
  short_description: "Audit GitHub Actions and release config for supply-chain risks"
  default_prompt: "Use $security-audit to scan this repo for supply-chain risks in GitHub Actions workflows and release configuration."
policy:
  allow_implicit_invocation: true
