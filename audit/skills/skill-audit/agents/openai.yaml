interface:
  display_name: "Skill Audit"
  short_description: "Judge whether a skill is fit to ship: discovery, instructions, context cost, portability, safety"
  default_prompt: "Use $skill-audit to gate the skills changed on this branch before committing."
policy:
  allow_implicit_invocation: true
